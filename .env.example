# Local SQLite is the default during development.
DATABASE_PROVIDER=sqlite
SQLITE_FILENAME=cosmic-pulse.db

# Production option for Vercel. Apply supabase/schema.sql first, then set
# DATABASE_PROVIDER=supabase and these server-only values in Vercel.
SUPABASE_URL=
SUPABASE_SECRET_KEY=
